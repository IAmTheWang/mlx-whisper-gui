# cmd/

Container directory for Go binary entrypoints. Currently holds a single binary, `whisper-gui/` — see its own `CLAUDE.md`. If a second binary (e.g. a CLI-only variant) is ever added, it goes in a sibling directory here, each with its own `main` package.
